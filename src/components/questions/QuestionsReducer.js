let initialState = {}

export default function QuestionsReducer(state = initialState, action) {
    let newState = Object.assign({}, state)
    switch (action.type) {
        default:
            break;
    }
    return newState
}